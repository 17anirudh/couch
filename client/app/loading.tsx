export default function () {
    return (
        <video 
            src="/loading.mp4" 
            autoPlay 
            loop 
            muted 
            className="absolute inset-0 w-full h-full object-cover -z-50"
        />
    )
}